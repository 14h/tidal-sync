{
  "name": "tidal-sync",
  "version": "1.0.2",
  "description": "Tidal playlist sync CLI — Master quality",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "tidal-sync": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "prepare": "npm run build",
    "start": "node dist/index.js",
    "release": "bash release.sh"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "chalk": "^5.3.0",
    "cli-progress": "^3.12.0",
    "commander": "^12.1.0",
    "node-taglib-sharp": "^5.2.3",
    "ora": "^8.1.0"
  },
  "devDependencies": {
    "@types/cli-progress": "^3.11.6",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
